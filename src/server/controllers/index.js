const suburbs = require('./suburbs');
const properties = require("./properties");
const inspections = require("./inspections")
const features = require("./features")
const root = require("./rootController")

module.exports = {
  suburbs,
  properties,
  inspections,
  features,
  root
};